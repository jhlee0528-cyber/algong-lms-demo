# 알공공부방 LMS v2.0 업그레이드 프롬프트

## 사전 작업
먼저 현재 v1.0 상태를 태그로 고정해줘:
```
git tag -a v1.0 -m "Simple Version 1.0 - 극도로 단순한 원장님 LMS"
git push origin demo
git push origin v1.0
```

## 개요
현재 v1.0의 `MainClassViewPage.tsx`를 v2.0으로 업그레이드한다.
핵심 원칙: "극도로 단순하게"는 유지하되, 시각적 품질과 사용성을 대폭 개선한다.

## 변경 사항 상세

### 1. 미니 대시보드 (화면 최상단)

테이블 위에 한 줄짜리 요약 바를 추가한다.

```
┌─────────────────────────────────────────────────────────┐
│ 🟢 정상 16명  🟡 주의 3명  🔴 관리필요 1명 │ 출석 15/20 │ 미발송 3명 │
└─────────────────────────────────────────────────────────┘
```

- 각 신호등 숫자는 클릭 시 해당 상태의 학생만 필터링
- 배경: 연한 회색 카드 스타일 (rounded, padding)
- 숫자는 실제 mockStudents 데이터에서 동적 계산

### 2. 필터 + 뷰 전환 바 (미니 대시보드 바로 아래)

```
[전체] [🟢 정상] [🟡 주의] [🔴 관리필요]          [📊 표] [🎴 카드]    [전체발송]
```

- 왼쪽: 신호등 필터 버튼 (토글 방식, 선택된 버튼은 배경색 강조)
- 오른쪽: 뷰 전환 토글 (표/카드)
- 기본 상태: "전체" + "표로보기"
- 필터 상태는 뷰 전환해도 유지

### 3. 표로보기 (기본 뷰) - v1.0 테이블 개선

v1.0의 기본 테이블 구조를 유지하되 다음을 개선:

#### 3-1. 학생 열 개선
- 성 이니셜 원형 아바타에 신호등 색상 테두리 적용
  - 🟢 정상: 초록색 테두리
  - 🟡 주의: 노란색 테두리
  - 🔴 관리필요: 빨간색 테두리 + 살짝 빨간 배경
- 이름 옆에 학년 표시

#### 3-2. 레벨 열에 프로그레스바 추가
- 레벨 숫자 아래에 얇은 프로그레스바 표시
- 진행률 = 해당 학생의 전체 과목 평균 진도율 (subjects 배열에서 계산)
- 바 색상은 신호등 상태에 따라 변경 (초록/노랑/빨강)

#### 3-3. 상태 열 개선
- 단순 원형 + 텍스트 대신, 배경색이 있는 뱃지(pill) 형태로
- 예: [🟢 정상] [🟡 주의] [🔴 관리필요]

#### 3-4. 관리필요(🔴) 학생 상단 고정
- status가 'delayed'인 학생은 항상 테이블 최상단에 표시
- 연한 빨간 배경으로 시각적 구분
- 그 아래에 '주의' 학생, 마지막에 '정상' 학생 순서

#### 3-5. 학생 행 클릭 시
- 기존 모달은 유지하되, 디자인 개선:
  - 상단: 아바타 + 이름 + 레벨 + 신호등 배지
  - 중단: 과목별 프로그레스바 (각 과목 이름 + 진도율 %)
  - 독서: 독서 레벨 + 읽은 권수 표시
  - 하단: 최근 활동 타임라인 (날짜 + 활동 내용 + 획득 코인)
  - 맨 아래: [학부모 리포트 보내기] 버튼

### 4. 카드로보기 (토글 뷰)

뷰 전환 버튼으로 [🎴 카드]를 선택하면 표시되는 뷰.

#### 4-1. 그리드 레이아웃
- PC: 4열 그리드 (gap: 12px)
- 태블릿(768px 이하): 2열
- 모바일(480px 이하): 1열

#### 4-2. 카드 구성
각 카드에 포함할 내용:
```
┌───────────────────────────┐
│  [김] ← 아바타 (신호등 테두리)
│  김민수  5학년
│  Lv.14            🟢 정상
│─────────────────────────── │
│  파닉스  ████████░░  80%  │
│  어휘    ██████░░░░  60%  │
│  문법    ████░░░░░░  40%  │
│─────────────────────────── │
│  📖 Lv.3 (12권, 이번달 4권)│
│                           │
│  [리포트 발송]             │
└───────────────────────────┘
```

- 과목별 프로그레스바: 해당 학생의 subjects 데이터에서 가져옴
- 바 색상: 70% 이상 = 초록, 40~70% = 노랑, 40% 미만 = 빨강
- 카드 클릭 시: 표 뷰와 동일한 상세 모달 열림
- 관리필요 카드: 빨간 테두리 + 연한 빨간 배경

#### 4-3. 정렬 순서
표 뷰와 동일: 관리필요 → 주의 → 정상 순서

### 5. 학부모 리포트 모달 개선

기존 `ParentReportModal.tsx` 디자인 개선:

#### 5-1. 카카오톡 스타일 미리보기
- 리포트 내용을 카카오톡 말풍선 형태로 표시
- 노란 배경 + 말풍선 UI (카카오 알림톡 느낌)
- 상단에 "알공 공부방" 발신자 표시

#### 5-2. 리포트 내용에 시각 요소 추가
- 학생 레벨 + 신호등 배지
- 주요 과목 진도율 바 (텍스트가 아닌 시각적 바)
- "이번 주 성장 포인트" 하이라이트 섹션

#### 5-3. 수정/발송 UX
- [수정하기] 누르면 텍스트 에디터로 전환
- [발송하기] 누르면 발송 애니메이션 → 완료 메시지

### 6. 모바일 반응형

#### 6-1. 전체 레이아웃
- 768px 이하: 사이드바 숨김, 햄버거 메뉴로 전환
- 480px 이하: 패딩 축소, 폰트 살짝 축소

#### 6-2. 표 뷰 모바일
- 768px 이하에서 테이블 열 축소:
  - 보이는 열: 학생(아바타+이름), 상태(신호등 배지), 발송 버튼
  - 숨기는 열: 레벨, 독서 (클릭 시 상세에서 확인)

#### 6-3. 카드 뷰 모바일
- 1열 풀와이드 카드로 자연스럽게 전환
- 카드 내 프로그레스바 유지

## 기술 요구사항

- 기존 React 18 + TypeScript + Tailwind CSS 스택 유지
- 새로운 라이브러리 설치 금지 (기존 의존성만 사용)
- 상태 관리: React useState/useEffect만 사용
- 뷰 전환 상태를 localStorage에 저장하여 새로고침 후에도 유지
- 모든 데이터는 기존 mockStudents.ts에서 가져옴

## 파일 변경 계획

수정 대상:
- `src/pages/MainClassViewPage.tsx` — 전면 재작성 (미니 대시보드 + 필터 + 뷰 전환)
- `src/components/report/ParentReportModal.tsx` — 카카오톡 스타일로 개선

신규 생성:
- `src/components/class-view/MiniDashboard.tsx` — 미니 대시보드 컴포넌트
- `src/components/class-view/FilterBar.tsx` — 필터 + 뷰 전환 바
- `src/components/class-view/TableView.tsx` — 개선된 표 뷰
- `src/components/class-view/CardView.tsx` — 카드 뷰
- `src/components/class-view/StudentDetailModal.tsx` — 개선된 학생 상세 모달

## 작업 순서

1. 먼저 현재 프로젝트 구조와 MainClassViewPage.tsx를 분석
2. MiniDashboard 컴포넌트부터 만들기
3. FilterBar 컴포넌트 (신호등 필터 + 뷰 전환)
4. TableView 개선 (기존 테이블 업그레이드)
5. CardView 신규 제작
6. StudentDetailModal 개선
7. ParentReportModal 카카오톡 스타일 개선
8. MainClassViewPage에서 모든 컴포넌트 통합
9. 모바일 반응형 적용
10. 빌드 테스트 후 커밋

## 커밋 메시지
```
feat: v2.0 수업보기 대폭 개선

- 미니 대시보드 추가 (학급 전체 요약)
- 표/카드 뷰 토글 기능
- 신호등 필터 (전체/정상/주의/관리필요)
- 관리필요 학생 상단 고정
- 카드 뷰: 과목별 프로그레스바, 반응형 그리드
- 학부모 리포트: 카카오톡 말풍선 스타일
- 모바일 반응형 (사이드바 숨김, 열 축소)
```

## 주의사항
- "극도로 단순하게" 원칙 유지 — 기능 추가가 아니라 기존 기능의 시각적 품질 향상
- Tailwind CSS 클래스를 적극 활용 (커스텀 CSS 최소화)
- 색상은 Tailwind 기본 팔레트 사용 (green-500, yellow-500, red-500 등)
- 컴포넌트를 잘 분리해서 나중에 개별 수정이 쉽도록
